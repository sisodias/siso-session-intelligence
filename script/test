#!/bin/sh
set -eu
REPO_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$REPO_ROOT/src" python3 -m unittest discover -s "$REPO_ROOT/tests" -v
PYTHONDONTWRITEBYTECODE=1 python3 "$REPO_ROOT/scripts/publication_check.py"
